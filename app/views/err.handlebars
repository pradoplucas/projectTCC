
{{#if logged}}
    {{> navbarOtherLog }}
{{else}}
    {{> navbarOtherNot }}
{{/if}}

<div class="mastdiv">
    <div class="container-fluid h-100">
        <div class="row h-100 align-items-center text-center">
            <div class="col-12 div-margin">
                <h1 class="fsc-2 mb-3">Houve um erro</h1>

                <div class="row justify-content-center text-center">
                    <div class="col-11 col-sm-10 col-md-9 col-lg-8 py-3 mb-0 box-custom">
                            <h4 class="px-sm-5 px-3 my-3">{{{message}}}</h4>
                            <a class="btn btn-primary my-3" href="{{pathReload}}">Recarregar página</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>